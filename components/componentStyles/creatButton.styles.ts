import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        position: "absolute",
        alignSelf: "center",
        top: "90%",
        elevation: 5,
    },
    icon: {
        fontSize: 20,
        color: "#000000",
    },
});
