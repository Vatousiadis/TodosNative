import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignSelf: "center",
        top: "35%",
    },
    typography: {
        color: "#f0f8ff",
        textAlign: "center",
        fontSize: 20,
    },
});
